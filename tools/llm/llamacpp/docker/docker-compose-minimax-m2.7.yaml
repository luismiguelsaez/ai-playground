---

services:
  # Minimax M2.7
  # Size: 229B - 10B active
  # HF: https://huggingface.co/unsloth/MiniMax-M2.7-GGUF
  # GPU(s): 2 x RTX 6000 Pro
  llamacpp-minimax:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda13
    pull_policy: always
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
    ports:
      - "8002:8080"
    volumes:
      - ~/.cache/huggingface/hub:/root/.cache/huggingface/hub:rw
    command: >
      --hf-repo unsloth/MiniMax-M2.7-GGUF:UD-Q4_K_M
      --alias coding-agent
      --ctx-size 196608
      --parallel 2
      --cache-type-k q8_0
      --cache-type-v q8_0
      --flash-attn on
      --repeat-penalty 1.05
      --temp 1.0
      --top-k 40
      --min-p 0.05
      --top-p 0.95
      --jinja
      --reasoning-format auto
      --gpu-layers 999
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0','1']
              capabilities: [gpu]
    restart: unless-stopped
