---

services:
  forge:
    build: ./forge
    ports:
      - "8877:7860"
    volumes:
      - ${HOME}/comfy/ComfyUI/models/checkpoints:/app/forge/models/Stable-diffusion
      - ${HOME}/comfy/ComfyUI/models/vae:/app/forge/models/VAE
      - ${HOME}/comfy/ComfyUI/models/embeddings:/app/forge/embeddings
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    restart: unless-stopped
    networks:
      - sd-network
  comfy:
    image: yanwk/comfyui-boot:cu128-slim-20251129
    ports:
      - "8188:8188"
    volumes:
      - ${HOME}/comfy/ComfyUI/models:/root/ComfyUI/models
      - ${HOME}/comfy/ComfyUI/input:/root/ComfyUI/input
      - ${HOME}/comfy/ComfyUI/output:/root/ComfyUI/output
      - ${HOME}/comfy/ComfyUI/user/default/workflows:/root/ComfyUI/user/default/workflows
      - ${HOME}/comfy/ComfyUI/custom_nodes:/root/ComfyUI/custom_nodes
    environment:
      CLI_ARGS: "--disable-xformers"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['0','1']
              capabilities: [gpu]
    restart: unless-stopped
    networks:
      - sd-network

networks:
  sd-network:
    driver: bridge

